\# USART收发数据



\## 工作原理



发数据是单片机发到电脑，一次最多8比特，通过一个字节一个字节连续发送发送更大的数据。
接收数据需要参考RXNE标志位，通过对其监控来接收数据，或者使用中断的方式：

第一步：配置GPIOTX引脚配置复用推挽输出，RX引脚配置浮空输入或上拉输入。
第二步：配置USART和普通串口配置一样，波特率、数据位、停止位、校验位，最后使能USART。
第三步：开启RXNE中断告诉USART收到数据时产生中断请求。
第四步：配置NVIC设置中断优先级，使能USART1的中断通道，让NVIC知道要响应这个中断。
第五步：写中断服务函数


